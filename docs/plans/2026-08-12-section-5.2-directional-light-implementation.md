# RTR4 Section 5.2 Directional Light Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a cited section 5.2 lighting lesson, runnable directional-light shader, and a minimal UI control that switches between lessons 5.1 and 5.2.

**Architecture:** Reuse the existing lesson JSON, shader metadata, API route, and `LessonPanel`. Add one immutable lesson bundle for section 5.2 and keep navigation state in `App`; no new backend abstraction or local shader runtime.

**Tech Stack:** FastAPI/Pydantic, React/Vite, Vitest, pytest, GLSL, ShaderToy-compatible GLSL.

---

### Task 1: Define the section 5.2 lesson contract

**Files:**
- Modify: `backend/tests/test_teaching.py`
- Create: `content/rtr4-cn/chapter-05/section-5.2.json`
- Create: `content/rtr4-cn/chapter-05/examples/directional-light.json`

1. Write a failing test that loads `section-5.2.json`, requires all seven lesson levels, verifies source citations from pages 106-109, and checks the directional-light shader metadata.
2. Run `python -m pytest backend/tests/test_teaching.py -q` and confirm failure because the bundle is absent.
3. Add the smallest cited lesson JSON and metadata satisfying the contract.
4. Re-run the focused test and confirm remaining failure is limited to absent shader files.
5. Commit the lesson contract and content metadata.

### Task 2: Add runnable directional-light examples

**Files:**
- Create: `content/rtr4-cn/chapter-05/examples/directional-light.frag`
- Create: `content/rtr4-cn/chapter-05/examples/directional-light-shadertoy.frag`
- Modify: `backend/tests/test_teaching.py`

1. Extend the failing test to require `normalize`, `dot`, and clamping in the example and ShaderToy entry points in the browser variant.
2. Run the focused test and confirm it fails for missing shader sources.
3. Add a minimal GLSL fragment shader plus a ShaderToy-compatible version showing a constant light direction across a sphere.
4. Run the focused pytest file and `glslangValidator -S frag content/rtr4-cn/chapter-05/examples/directional-light.frag`.
5. Commit the runnable example.

### Task 3: Make section 5.2 reachable in the local Web app

**Files:**
- Modify: `frontend/src/App.test.tsx`
- Modify: `frontend/src/App.tsx`

1. Write a failing UI test requiring controls for sections 5.1 and 5.2 and verifying the selected slug passed to `LessonPanel`.
2. Run `npm test -- --run src/App.test.tsx` from `frontend` and confirm the new test fails.
3. Add local section selection state and two accessible buttons; keep 5.1 as the default.
4. Re-run the focused frontend test.
5. Commit the UI navigation.

### Task 4: Verify and publish the increment

1. Run backend teaching tests, full backend tests, Ruff, frontend tests, frontend production build, and both GLSL validators.
2. Confirm `git status --short` contains no generated PDF, model, cache, image, SQLite, or MinerU artifacts.
3. Push `feature/rtr4-learning` without force.
4. Report the new commit hashes and cumulative feature-branch commit count.
