# RTR4 Section 5.2.2 Punctual Lights Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a cited lesson and runnable shader that teach point lights, inverse-square distance falloff, finite light ranges, and spotlights from RTR4 section 5.2.2.

**Architecture:** Reuse the existing lesson JSON, shader metadata, API route, and lesson selector. Publish one new immutable `section-5.2.2` bundle with a single GLSL example that compares a point light and a spotlight; ShaderToy remains the browser execution surface.

**Tech Stack:** FastAPI/Pydantic, React/Vite, Vitest, pytest, GLSL, ShaderToy-compatible GLSL.

---

### Task 1: Add the punctual-light lesson contract

**Files:**
- Modify: `backend/tests/test_teaching.py`
- Create: `content/rtr4-cn/chapter-05/section-5.2.2.json`
- Create: `content/rtr4-cn/chapter-05/examples/punctual-lights.json`

1. Write a failing test that loads `section-5.2.2.json`, requires all seven lesson levels, validates citations from pages 109-114, and checks point/spotlight metadata.
2. Run the focused pytest test and confirm it fails because the bundle is absent.
3. Add the minimum cited lesson and metadata covering formulas 5.9, 5.11, 5.12/5.13, 5.14, 5.17, and 5.18.
4. Re-run the focused test and confirm any remaining failure is limited to absent shader files.
5. Commit lesson contract and metadata.

### Task 2: Add point-light and spotlight shaders

**Files:**
- Create: `content/rtr4-cn/chapter-05/examples/punctual-lights.frag`
- Create: `content/rtr4-cn/chapter-05/examples/punctual-lights-shadertoy.frag`
- Modify: `backend/tests/test_teaching.py`

1. Require distance computation, stabilized inverse-square falloff, a finite-range window, and smooth spotlight attenuation in the test.
2. Confirm RED because sources are absent.
3. Add minimal GLSL: left sphere uses a point light; right sphere uses the same punctual light with a smooth spotlight cone. The ShaderToy version uses `iResolution` and `mainImage` only.
4. Run focused pytest and compile the desktop fragment shader with `glslangValidator`.
5. Commit runnable examples.

### Task 3: Expose section 5.2.2 in the Web app

**Files:**
- Modify: `frontend/src/App.test.tsx`
- Modify: `frontend/src/App.tsx`

1. Add a failing test that selects `5.2.2 精确光源` and observes `section-5.2.2` passed to `LessonPanel`.
2. Run the focused Vitest file and confirm RED.
3. Add one accessible selector button; preserve existing defaults and navigation behavior.
4. Run focused frontend tests.
5. Commit UI navigation.

### Task 4: Verify and publish

1. Run all backend tests and Ruff.
2. Run all frontend tests and production build.
3. Compile Gooch, directional-light, and punctual-lights desktop shaders.
4. Audit tracked files for PDFs, models, caches, generated PNGs, SQLite, MinerU data, `dist`, and `node_modules`.
5. Push `feature/rtr4-learning` without force and report new commits plus cumulative count.
